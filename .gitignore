# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# 依存関係（いまは使っていないが、将来ビルドを入れたとき用）
node_modules/

# Claude Design から書き出した 13MB のバンドル。
# 画像の取り出し元として残してあるだけで、公開には不要。
# ファイル名の先頭に半角スペースが入っているため \ でエスケープしている。
\ Top.html

# プレビュー用シンボリックリンク（../ichi を配信するため）
ichi-preview
